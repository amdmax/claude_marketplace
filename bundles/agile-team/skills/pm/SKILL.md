---
name: pm
description: "Project manager — owns story lifecycle, delivery tracking, AC enrichment, and story state transitions. Supports story-extract and story-validate subcommands."
tools:
  - Skill
---

# PM

Invoke the `agent:pm` skill, passing all ARGUMENTS through unchanged.

- `/pm` → full PM workflow
- `/pm story-extract` → extract active story to YAML
- `/pm story-validate` → validate story-extract.yaml
